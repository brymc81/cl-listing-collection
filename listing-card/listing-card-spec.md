# Legacy Listing Card Spec (Deprecated)

Status: deprecated and not active in the `cl-listing-carousel` render path.

Current authority for active shared card rendering:

- `../cl-property-components/docs/card-contract.md`
- `docs/carousel-contract.md`

Notes:

- `cl-listing-carousel` now renders cards via `clpc_render_property_card`.
- Active card markup uses `.clpc-*` classes.
- The legacy `listing-card/*` artifacts are retained only for backward-compatibility history and local legacy tests.
